# 言灵 · Language RPG

一个由大语言模型驱动的中文文字 RPG / 互动小说前端。

玩家选择故事大纲、角色出身、世界书与随机事件后，由多个模型协作推进旅程：

- **故事模型**：根据设定、历史、长期记忆、背包、人物关系、场景与玩家行动生成正文。
- **决策模型**：根据最新故事提取选项、道具变化、人物关系、场景、时间与天气。
- **记忆模型**：周期性整理外观、服装、承诺、线索、关系等长期一致性信息。
- **摘要模型**：在历史过长时压缩早期上下文。
- **执笔辅助模型**：在执笔模式下提供设定守护、叙事导演、逻辑审校与剧情驱动长线事件；当前复用已有模型配置，无需单独配置额外接口。

项目是纯前端应用，所有设置、存档与自定义内容默认保存在浏览器 `localStorage` 中。

## 主要功能

- 导入或调用模型生成故事大纲、角色出身、世界书、随机事件与开局场景。
- **游历模式**：模型根据世界观生成故事正文和行动选项，玩家通过选择分支推进旅程；可配置每隔若干回合获得一次自由输入机会，支持有限回合与无尽模式。
- **执笔模式**：面向更强自定义与共同写作，默认每回合自由输入；可通过世界书、长期记忆、详细大纲、叙事导演、设定守护者与逻辑审校维持故事方向和设定一致性。
- 支持背包道具、NPC 好感与细节、场景跳转、时间天气、记忆锚点、聊天记录编辑和重新生成。
- 支持导出聊天记录 Markdown，以及可分享 / 导入的完整旅程包 JSON。

## 特色功能

- 高度可自定义的故事流程：严格自定义提示词、详细大纲、执笔模式独立提示词链路。
- 执笔模式辅助链路：
  - 叙事弧 / 剧情驱动长线随机事件。
  - 故事生成前置设定守护。
  - 环境侧主动反应建议。
  - 玩家偏好分析。
  - 逻辑审校与连续性修复建议。
  - 随故事推进的世界书候选补充系统。
- 模型辅助生成世界书、出身、剧情大纲、随机事件与开局。
- 随故事更新的场景、世界状态、时间、天气与长期记忆。
- 背包道具系统与随故事变化的人物关系。

## 快速开始

### Windows

双击：

```bat
start.cmd
```

首次启动会自动执行 `npm install`。

### macOS / Linux

```bash
chmod +x start.sh
./start.sh
```

启动后访问：

```text
http://127.0.0.1:5173
```

## 首次使用

1. 进入「设置」。
2. 填写：
   - API Base URL
   - API Key
   - 请求格式
   - 故事模型
   - 决策模型
3. 按需填写：
   - 摘要模型
   - 长期记忆模型
   - 随机生成模型
4. 返回主页，点击「启程 · 开始新旅程」。

> API Key 只保存在本地浏览器 `localStorage`，不会被项目主动上传到其他地方。

## 推荐模型配置

推荐：`deepseek-v4-pro` / `deepseek-v4-flash` 或能力相近的长上下文模型。

- **故事模型**：`deepseek-v4-pro` —— 故事生成，越强越好。
- **决策模型**：`deepseek-v4-flash` —— 场景轮换、人物关系、背包道具。
- **摘要 / 记忆模型**：`deepseek-v4-flash` —— 压缩上下文。
- **随机生成模型**：`deepseek-v4-pro` —— 生成世界书、人设、随机事件，越强越好。

## 存档与数据

本项目主要使用 Zustand persist 写入浏览器本地存储：

- `lrpg.settings`：模型 API、温度、记忆设置、故事风格等。
- `lrpg.games`：存档、聊天记录、背包、NPC、场景、长期记忆等。
- `lrpg.content`：自定义故事大纲、出身、世界书、随机事件。

如果更换浏览器、清理站点数据或换设备，本地数据可能丢失。建议使用「旅程包」导出重要存档。

## 内容导入 / 导出

- 「书库」中可以管理故事大纲、出身、世界书、随机事件。
- 「游戏页」可以导出：
  - 聊天记录 Markdown
  - 完整旅程包 JSON
- 「主页」可以导入旅程包 JSON。

旅程包不包含 API Key，但会包含当前旅程需要复现的故事设置、书库资源、执笔模式配置、严格自定义配置与聊天记录。

## 技术栈

- Vite
- React 18
- TypeScript
- Tailwind CSS
- Zustand
- React Router
- React Markdown

## 本地开发

```bash
npm install
npm run dev
npm run build
npm run preview
```

PowerShell 中如果直接运行 `npm` 被执行策略拦截，可使用：

```bat
cmd /c npm run build
```

## 项目结构

```text
src/
  pages/        页面级流程：主页、启程、游戏、设置、书库
  components/   UI 与游戏面板组件
  store/        Zustand 状态与 localStorage 持久化
  services/     模型调用与游戏服务
  prompts/      故事、决策、记忆、摘要、评分、执笔辅助等提示词
  presets/      内置故事大纲、出身、世界书、随机事件
  lib/          通用工具、严格自定义、旅程包等
  types/        TypeScript 类型定义
```

## 本地 API 联调

```bash
node scripts/test-api.mjs [model]
```

该脚本用于本地 OpenAI 兼容代理联调。长期使用时建议改为从环境变量读取 API Base 与 Key。

## 其他

此项目为纯 vibe coding 产物。

所用模型：

```text
初期&前端&prompt：opus4.7
维护：gpt5.5
```

## 即将实现

- 自定义功能仍在重构中，后续会继续向更贴合小说的稳定叙事方向调整。
- 加入伏笔和填坑功能，向更完整的小说结构靠拢。
- 将部分“固定回合制进度”改为更弹性的阶段制，以匹配不同玩家的叙事节奏。
- 主弧、关系分析、时间线 / 场景连续性等执笔辅助模型继续拆分。
- 设定守护者候选词条的批量审核与更精细的书库沉淀 UI。
- 更完善的提示词链路编辑器和块级长度预算。
- 重构更精美的前端表现。

## License

MIT
